import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://data-portfolio-website-ploypairaohpats-projects.vercel.app', // TODO: set to your real domain (drives sitemap + canonical URLs)
})
